# specs目录说明

## 目的

- `specs/` 用于存放可迭代的 spec 套件（`spec.md` / `tasks.md` / `checklist.md`）。
- 每个子目录对应一个独立版本或主题，便于多团队并行协作。

## 当前套件

- `specs/quality_optimization_v1/`：质量优化与治理闭环套件
- `specs/frontend_delivery_handoff_v1/`：前端交付衔接套件（外部团队产出）
